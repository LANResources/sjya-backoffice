St. Joseph Youth Alliance BackOffice
= "===================================="

= "Dear #{@user.full_name},"


= "#{@user.inviter.full_name} has invited you to become a registered user of the #{link_to 'St. Joseph Youth Alliance BackOffice', root_url}. "

Follow the link below to create your account.

= link_to "Register!", rsvp_user_url(@user.id, @user.invite_token)
